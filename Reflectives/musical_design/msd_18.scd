// Musical Sound Design 18 : MIDI controlled instruments with FX cascade and GUI

MIDIIn.connectAll;


s.latency = nil;            // for immediate response;

MIDIdef.trace(true);
MIDIdef.trace(false);

s.boot;
